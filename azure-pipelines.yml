# Node.js with React
# Build a Node.js project that uses React.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
  displayName: 'npm install and build'
- task: FtpUpload@2
  inputs:
    credentialsOption: 'inputs'
    serverUrl: 'ftp://xfer.lkqcorp.com'
    username: 'PgwagAbcQua'
    password: 'hRtYuVmxrgst'
    rootDirectory: './build'
    filePatterns: '**'
    remoteDirectory: '/incoming/$(Build.BuildId)/'
    clean: false
    cleanContents: false
    preservePaths: false
    trustSSL: false

- task: AzureFileCopy@3
  inputs:
    SourcePath: './build'
    azureSubscription: 'Visual Studio Enterprise(503ca594-6989-4938-bc47-4751dd81a8e9)'
    Destination: 'AzureBlob'
    storage: 'burghindianappf8fdc'
    ContainerName: 'bughindian-power-admin'